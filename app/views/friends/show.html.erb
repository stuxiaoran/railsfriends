<%# <p style="color: green"><%= notice %></p>

<div class="p-5 mb-4 bg-body-tertiary rounded-3">
  <div class="container-fluid py-5">
    <h1 class="display-5 fw-bold">Show Friend</h1>
    <p class="col-md-8 fs-4"><%= render @friend %></p>
    <%# <button class="btn btn-primary btn-lg" type="button">Example button</button> %>

    <%# <% if @friend.user == current_user %>
      <%= link_to "Edit this friend", edit_friend_path(@friend), class:"btn btn-secondary" %> 
    <%# <% end %>
    <%= link_to "Back to friends", friends_path, class:"btn btn-dark" %>

    <br/><br/>
    <%# <% if @friend.user == current_user %>
      <%= button_to "Delete this friend", @friend, method: :delete, class:"btn btn-danger" %>
    <%# <% end %>
  </div>
</div>

